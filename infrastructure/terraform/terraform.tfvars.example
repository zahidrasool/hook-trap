# --- Account safety: set these FIRST ---------------------------------------
# Named profile from ~/.aws/credentials. Avoids deploying with whatever
# credentials happen to be the default.
aws_profile = "mocklane"

# Your own AWS account ID. Terraform aborts before creating anything if the
# credentials resolve to a different account.
allowed_account_ids = ["111122223333"]

